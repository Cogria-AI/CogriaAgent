{
  "name": "cogria-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Business-agnostic conversational agent framework extracted from a SaaS merchant agent.",
  "license": "MIT",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "type-check": "pnpm -r type-check"
  }
}
